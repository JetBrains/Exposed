services:
    oracle:
        container_name: oracleDB
        restart: always
        image: container-registry.oracle.com/database/free:latest-lite
        ports:
            - "3003:1521"
        environment:
            ORACLE_PWD: "Oracle18"
